import config from './gulp/config';

config.setEnv();

export const build = () => {
  console.log(config.isProd);
  var
};

export const watch = () => {
  console.log(config.isProd);
  console.log(config.isProd);
};
